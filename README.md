# notion-expenses-visualizer

To use the project:
1. Create a virtual environment: `virtualenv venv`
2. Activate virtual environment: `source venv/bin/activate` (MAC)
3. Download the dependancies: `pip install -r requirements.txt`
